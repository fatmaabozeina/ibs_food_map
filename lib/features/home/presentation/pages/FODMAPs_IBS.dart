import 'package:flutter/material.dart';

class FODMAPsAndIBS extends StatelessWidget {
  const FODMAPsAndIBS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
